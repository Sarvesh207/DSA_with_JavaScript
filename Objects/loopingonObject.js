let x = {
    name:"john",
    company:"microsoft",
    salary:"20000",
    age:40,
    city:"bangluru",
}

let keysOfX = Object.keys(x); // will return an array of keys of x 
// console.log(keysOfX)

let valuesOfX = Object.values(x)   // will return an array of value of x
// console.log(valuesOfX)

let entriesOfx = Object.entries(x)
console.log(entriesOfx)
